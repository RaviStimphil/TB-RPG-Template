using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PassiveConditions : MonoBehaviour
{
    // Start is called before the first frame update
    public string[] elementHitBy; //If the passive owner is hit by element.
    public string[] elementAttackedWith; //If passive owner attacked with an element.
    


    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
